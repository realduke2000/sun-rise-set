
** 实现一个日出日落时间本地网页
Constraints:
- 直接用浏览器打开可浏览
- 日出日落的数据要嵌在html中
- 
Deliverables:
- 一个页面列出一年以内，北京的日出日落时间
- 在网站的中间设计一个天文图片，在数据的日出列，显示日出图片(本地图片sunrise.jpeg)，日落列显示日落图片(本地图片sunset.jpg)
- 在页面打开的时候，数据要逐渐的展示出来，像是一边读取一边刷新页面一样，最下方加一个齿轮图标，在数据的刷新过程中，一直转动，到数据刷新完成后消失，同时齿轮上方加一行文字，"数据抓取中...“,最后的"..."要反复从1个到3个的刷新，在数据刷新完成后，显示"数据抓取完成"
- 页面一直下拉滚动，始终显示最后的数据

** 实现一个日出日落时间的可视化网页，：
Constraints:
- 直接用浏览器打开可浏览
- 数据结构请使用对象数组，结构为 {date:"2026-01-01", sunrise:"07:38", sunset:"16:57", day_length:"09:19"， solar_term:"节气"}
- 
Deliverables:
- 一个包括不同的区域的页面，每个区域显示不同维度的日出、日落和白昼时长
- 第一个区域，标题为"2025年北京城区日出和日落时刻"，横轴是每个月1日的日期，纵轴是每天24小时时间点，画两个折线，一个折线是日出时间，一个折线是日落时间
- 第二个区域，标题为"2025年北京24节气日出日落昼长图"，横轴是24节气时间时间点，纵轴是每天24小时的时间点，两个折线和一个柱状图，一个折线是日出时间，一个折线是日落时间，底下的柱状图表示昼长

** 实现一个python自动化脚本，模拟数据读取和表格、统计数据生成
Constraints:
- 本地网页使用subprocess和退出，firefox的路径是"/usr/bin/firefox"

Deliverables:
1. 打开本地网页datagrid.html,等待3-5秒，同时持续输出后台随机日志，输出抓取完成的日之后关闭
2. 在控制台逐行打印正在读的取日出日落数据，日志格式要规范
3. 日志内容随机生成，例如读取某网站，有错误在重试，读取到那些数据，一共读取了哪些数据等等
4. 打开本地网页statistics.html